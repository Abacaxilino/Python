# print() — exibe algo na tela
# input() — recebe entrada do usuário
# len() — conta o tamanho de uma lista ou string
# int(), 
# str(), 
# float() — convertem tipos de dados
# bool() — converte para booleano
# type()) — mostra o tipo do dado

# r : Para ler um arquivo, precisamos abri-lo no modo somente leitura usando 'r'.
# w : Para escrever em um arquivo existente. Substituirá tudo o que já existe no arquivo. Se não houver arquivos existentes, criará um novo arquivo com o nome fornecido.
# r+ : Para ler e gravar dados em um arquivo, mas os dados existentes serão apagados e novos dados serão gravados.
# w+ : É usado para gravar e ler dados em um arquivo, mas todos os dados existentes são apagados e novos dados são gravados e armazenados.
# a : O modo de anexação é usado para adicionar novos dados ao final do arquivo sem apagar os dados existentes.
# a+ : O modo de anexação e leitura é usado para ler e adicionar novos dados    ao final do arquivo sem apagar os dados existentes.
# if — inicia uma estrutura condicional
# else — inicia o bloco alternativo de uma estrutura condicional
# elif — inicia uma nova condição em uma estrutura condicional
# while — inicia um loop de repetição
# for — inicia um loop de repetição para iterar sobre uma sequência
# break — interrompe um loop
# continue — pula para a próxima interação de um loop
# def — define uma função
# return — retorna um valor de uma função
# import — importa um módulo
# from — importa partes específicas de um módulo
